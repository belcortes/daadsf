<!-- <div class="modal fade in" id="contact">
  <div class="modal-header">
    <button class="close" data-dismiss="modal">x</button>
    <h2>Contact</h2>
  </div>
  <div class="modal-body">
    <%= simple_form_for @contact, :html => {:class => 'form-horizontal', "data-type" => :json }, remote: true do |f| %>
    <%= f.input :name, :required => true %>
    <%= f.input :email, :required => true %>
    <%= f.input :message, :as => :text, :required => false, :input_html => {:rows => 10} %>
    <div class='hidden'>
      <%= f.input :nickname, :hint => 'Leave this field blank!' %></div>
    </div>
    <div class='form-actions'>
      <%= f.button :submit, 'Send message', :class=> "btn btn-primary" %></div>
    </div>
  </div>
  <% end %>
</div> -->