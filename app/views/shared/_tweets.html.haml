%section#tweets
  %h3 Tweets
  .height
    %ul
      - Tweet.all.last(50).each do |t|
        %li
          .black
            @
            = t.handle
            %img(src="#{t.photo}")
          .tweet-text
            = t.text
        %br
        %br