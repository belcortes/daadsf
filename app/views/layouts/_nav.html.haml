#nav
  %a(href='/')
    =image_tag asset_path('AppaLogo.png'), :id => 'nav-img'
  #upper-nav
    #logo-name 
      DISASTER ASSESSMENT AND ASSISTANCE DASHBOARD
      %span#sf SF
      %span#beta BETA
    %ul#logo-nav
      %li
        - if current_user
          = current_user.email #, '#edit', "data-toggle" => "modal", :remote => true, :id => 'login_form'
        - else
          = link_to "BETA Login", "#login", "data-toggle" => "modal", :remote => true
      - if current_user
        %li#myaccount
          %a(href='#') My Account
      %li
        - if current_user
          = link_to "Logout", destroy_user_session_path, :method => :delete
        - else
          / #signin Sign Up
          = link_to "Sign up", "#sign_up", "data-toggle" => "modal", :remote => true, :id => 'signup'
      %li
        - if current_user && current_user.admin?
          = link_to "Breaking", "#breaking", "data-toggle" => "modal", :remote => true
  %ul#subnav
    %li.login#prepare
      %a(href='#')
        .map-buttons Prepare for a Disaster
    %li.login#print
      =image_tag asset_path("print.png"), id: 'print'
    / %li.login.ticker
    /   %span#breaking BREAKING
    /   %ul#ticker
    /     %li.news-list This is the 1st latest news item.
    /     %li.news-list This is the 2nd latest news item. plus some more things i want to write
    /     %li.news-list This is the 3rd latest news item.
    /     %li.news-list This is the 4th latest news item.
      
= render "home/login_modal"
= render "home/sign_up_modal"
= render "home/edit_modal"
= render "home/breaking"