#preparation
  %h3 Printing Options

  = link_to "All Resources", '#', onclick: "gmapPrint()"
  %p Local Resources by Zipcode
  - User.uniq.pluck(:zip).each do |p|
    = link_to "#{p}", "/print/#{p}", class: 'zip-click'
    %br

